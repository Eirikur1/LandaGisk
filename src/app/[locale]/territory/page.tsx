import TerritoryFlags from "@/components/games/TerritoryFlags";

export default function TerritoryPage() {
  return (
    <div className="relative z-40 min-h-[calc(100vh-5rem)] overflow-hidden">
      <TerritoryFlags />
    </div>
  );
}
